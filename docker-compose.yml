services:
  lake-time-series-forecasting:
    build: .
    container_name: lake-time-series-forecasting
    init: true
    ports:
      - "8501:8501"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
